FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (including devDependencies for build)
RUN npm install

# Copy source code
COPY . .

# Build (static export is automatic with output: 'export')
RUN npm run build

# Default command
CMD ["echo", "Build completed. Check /app/out directory"]